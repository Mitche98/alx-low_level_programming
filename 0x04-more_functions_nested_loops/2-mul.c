#include "main.h"

/**
 * mul - function to multiply 2 int
 * @a: first int to be checked
 * @b: second int to be checked
 * Return: addititon of the both char
 */

int mul(int a, int b)
{
	return (a * b);
}
